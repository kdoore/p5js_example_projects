//declare global variables
//setup to initialize global variables and call functions which are only executed once
setup = function() {
    var div1 = createDiv('');
    div1.id = "canvasContainer";
    var canvas1 = createCanvas(400, 400);
    canvas1.parent('canvasContainer');
}
draw = function() {
    
}